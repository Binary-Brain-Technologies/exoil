/**
 * Tracking plan (docs/legal-launch-requirements.md §4). Nothing is loaded unless enabled here AND
 * documented in the approved privacy policy.
 */
export const tracking = {
  vercelWebAnalytics: false,
  googleAnalytics: false,
  metaPixel: false,
} as const;
